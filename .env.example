# open-db-mcp 环境变量示例
# 复制到 ~/.open-db-mcp/.env 或项目根目录 .env 后按需修改

# 数据源配置文件路径（默认 config/datasources.json）
MCP_DATASOURCES_CFG_PATH=config/datasources.json

# 连接池
MCP_POOL_MAX=8

# 单次 DML 最大影响行数
MCP_MAX_AFFECTED_ROWS=1000

# 审计日志
MCP_AUDIT_LOG_PATH=~/.open-db-mcp/audit.jsonl
